require "display"
require "board"

describe Display do

    # xit "" do
    #     board = Board.new.board
    #     new_display = Display.new(board)
    #     expect(new_display).to eq("   |   |   \n-----------\n       |   |    \n    -----------\n       |   |   \n")
    # end

    # context "#array_string" do 
    #     it "converts the board array into a string of nine characters" do
    #         board = Board.new.board
    #         new_display = Display.new(board)
    #         expected = new_display.array_string
    #         expect(expected).to eq("---------")
    #         expect(expected.length).to eq(9)
    #     end
    # end

    # context "#split_to_row" do 
    #     it "split string into rows" do
    #         board = Board.new.board
    #         new_display = Display.new(board)
    #         expected = new_display.split_to_row(new_display.array_string)
    #         expect(expected).to eq("---\n-----------\n---\n---\n")
    #         # expect(expected).to eq("   |   |   \n-----------\n       |   |    \n    -----------\n       |   |   \n")
    #     end
    # end

end